[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — wouter client routing icin
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
